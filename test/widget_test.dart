import 'package:flutter_test/flutter_test.dart';

import 'package:zentask/main.dart';

void main() {
  test('ZentaskApp can be constructed', () {
    expect(const ZentaskApp(), isA<ZentaskApp>());
  });
}
